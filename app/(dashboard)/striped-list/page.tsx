import { StripedListPage } from "@/app/components/blocks/striped-list-page";

export const metadata = { title: "Striped list · Nuxem", description: "Striped data table" };

export default function StripedListRoute() {
  return <StripedListPage />;
}
