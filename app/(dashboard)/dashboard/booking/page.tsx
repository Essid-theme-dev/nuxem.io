import { BookingDashboard } from "@/app/components/blocks/booking-dashboard";

export const metadata = { title: "Booking · Dashboard", description: "Booking dashboard variant" };

export default function BookingDashboardPage() {
  return <BookingDashboard />;
}
