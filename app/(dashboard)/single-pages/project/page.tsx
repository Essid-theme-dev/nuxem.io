import { SingleDetailPage } from "@/app/components/blocks/single-detail-page";

export const metadata = { title: "Project · Single", description: "Project single page layout" };

export default function SingleProjectPage() {
  return <SingleDetailPage variant="project" />;
}
